namespace Lombiq.ChartJs.Constants
{
    public static class ResourceNames
    {
        public const string Library = FeatureIds.Area;
        public const string Annotation = Library + "." + nameof(Annotation);
        public const string DataLabels = Library + "." + nameof(DataLabels);
    }
}
